service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write;
      // allow read: if request.auth != null;
      // allow write: if true;
      // allow read: if true;
    }
  }
}